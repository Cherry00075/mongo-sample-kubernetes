apiVersion: networking.k8s.io/v1Beta1
kind: Ingress
metadata:
    name: dashboard-ingress
    namespace: kubernetes-dashboard
spec:
 rules:
    host: dashboard.com
    http:
     paths:
      backened:
       serviceName: 
